MYCALL ZR1RF-10
ADEVICE plughw:0,0
MODEM 1200

PBEACON delay=0 every=60 via=WIDE2-2 lat=-33.8421 long=18.6946 symbol=Wa comment="144.575MHz Winlink Packet Gateway"

# --- Timing parameters ---
TXDELAY 200       # Wait 200 ms after PTT before sending data
TXTAIL  20        # Keep PTT active 20 ms after last byte
DWAIT   0         # No extra delay before transmission
SLOTTIME 10       # For CSMA (not critical for point-to-point)
PERSIST 63        # Default value for channel access

CHANNEL ZR1RF-10 ax0 1200

